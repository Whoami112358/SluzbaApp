@{
    <link rel="stylesheet" href="https://db.onlinewebfonts.com/c/7aa6f23718712abc71a57b079ebadb52?family=Korolev+Military+Stencil+W00+Rg">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">

}
<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista Żołnierzy - Twoja Aplikacja</title>
    <link rel="stylesheet" href="~/css/site.css" />
    <style>
        body {
            font-family: 'Poppins', Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-image: linear-gradient(45deg, rgba(3, 34, 40, 0.6) 0%, rgba(15, 24, 16, 0.6) 100%), url('../zolniez.png');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            color: #ecf0f1;
            display: flex;
            flex-direction: column;
            height: 100vh;
        }

        header {
            text-align: center;
            padding: 20px 0;
        }

        h1 {
            font-family: 'Korolev Military Stencil W00 Rg';
            font-size: 48px;
            margin: 25px 0;
            color: #ffffff;
        }


        footer {
            text-align: center;
            padding: 10px;
            color: #ffffff;
            font-size: 14px;
            position: relative;
            margin-top: auto;
        }

            footer button {
                position: absolute;
                bottom: 10px;
                left: 10px;
                border: none;
                background: none;
                padding: 0;
                cursor: pointer;
            }

                footer button img {
                    width: 25px;
                    height: 24px;
                    display: block;
                }

        .content {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: auto; /* Przesuwalność zawartości */
            padding: 20px;
        }

        table {
            width: 90%;
            max-width: 800px;
            border-collapse: collapse;
            margin: auto; /* Wyśrodkowanie tabeli */
            overflow: auto; /* Przesuwalność tabeli */
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            background-color: rgba(172, 174, 165, 0.7);
            border-radius: 8px; /* Zaokrąglone rogi */
        }

            table th, table td {
                padding: 10px;
                text-align: left;
                border: 1px solid #ddd;
            }

            table th {
                background-color: rgba(119, 131, 83, 1);
                color: white;
            }

            table td {
                background-color: rgba(172, 174, 165, 0.7);
            }

        form {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /*form input[type="number"] {
                padding: 5px;
                margin-right: 10px;
                width: 50px; /* Większe pole wejściowe 
            }

            form button {
                padding: 5px 10px;
                background-color: rgba(119, 131, 83, 1);
                border: none;
                color: white;
                cursor: pointer;
                transition: background-color 0.3s ease;
            }

                form button:hover {
                    background-color: rgba(0, 0, 0, 0.2);
                }*/

        form button {
            margin-left: 10px;
            margin-right: 10px;
            padding: 5px 10px;
            width: 40px; /* Make buttons square */
            height: 40px;
            font-size: 30px; /* Big font size for plus and minus */
            font-weight: bold;
            color: white;
            background-color: rgba(119, 131, 83, 1);
            border-radius: 10px; /* Rounded corners */
            cursor: pointer;
            display: flex;
            justify-content: center;
            align-items: center;
            transition: background-color 0.3s ease, transform 0.2s ease; /* Smooth transition for hover */
        }

            /* Hover effect */
        form button:hover {
                background-color: rgba(0, 0, 0, 0.2);
                transform: scale(1.1); /* Slightly enlarge on hover */
            }

            /* Active (clicked) state for a button */
        form button:active {
                    background-color: rgba(0, 0, 0, 0.2);
                transform: scale(1); /* No scale on click */
            }

            /* Optional: Focus outline for accessibility */
        form button:focus {
                outline: 3px solid #333; /* Focus border */
            }
                }

        th a {
            display: inline-block;
            text-decoration: none;
            margin: 0 2px;
        }

        .fa-arrow-up, .fa-arrow-down {
            font-size: 14px;
            color: #000000;
            margin-left: 5px;
            cursor: pointer;
        }

            .fa-arrow-up:hover, .fa-arrow-down:hover {
                color: #000000;
            }
    </style>
</head>
<body>
    <header>
        <h1>LISTA ŻOŁNIERZY</h1>
    </header>
    <div class="content">
        <table>
            <thead>
                <tr>
                    <th>
                        Imię
                        <a href="@Url.Action("Punktacja", "Dyzurny", new { sortBy = "Imie", isDescending = false })" style="margin-left: 5px;">
                            <i class="fa fa-arrow-up"></i>
                        </a>
                        <a href="@Url.Action("Punktacja", "Dyzurny", new { sortBy = "Imie", isDescending = true })">
                            <i class="fa fa-arrow-down"></i>
                        </a>
                    </th>
                    <th>
                        Nazwisko
                        <a href="@Url.Action("Punktacja", "Dyzurny", new { sortBy = "Nazwisko", isDescending = false })" style="margin-left: 5px;">
                            <i class="fa fa-arrow-up"></i>
                        </a>
                        <a href="@Url.Action("Punktacja", "Dyzurny", new { sortBy = "Nazwisko", isDescending = true })">
                            <i class="fa fa-arrow-down"></i>
                        </a>
                    </th>
                    <th>Stopień</th>
                    <th>
                        Punkty
                        <a href="@Url.Action("Punktacja", "Dyzurny", new { sortBy = "Punkty", isDescending = false })" style="margin-left: 5px;">
                            <i class="fa fa-arrow-up"></i>
                        </a>
                        <a href="@Url.Action("Punktacja", "Dyzurny", new { sortBy = "Punkty", isDescending = true })">
                            <i class="fa fa-arrow-down"></i>
                        </a>
                    </th>
                    <th>Dodaj/Usuń Punkty</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Imie</td>
                        <td>@item.Nazwisko</td>
                        <td>@item.Stopien</td>
                        <td>
                            <form id="punktyForm" method="post" action="/Dyzurny/ZaktualizujPunkty">
                                <input type="hidden" name="ID_Zolnierza" value="@item.ID_Zolnierza" />
                                <input id="punkty" type="hidden" name="punkty" step="1" value="0"/>
                                <button type="button" onclick="updatePunkty(this, 1)">+</button>
                                @item.Punkty
                                <button type="button" onclick="updatePunkty(this, -1)">-</button>
                            </form>

                            <script>
                                function updatePunkty(button, changeValue) {
                                    // Find the parent form of the button clicked
                                    let form = button.closest('form');

                                    // Get the 'punkty' input field within that form
                                    let punktyInput = form.querySelector('#punkty');

                                    // Update the value by adding or subtracting the changeValue
                                    punktyInput.value = parseInt(punktyInput.value) + changeValue;

                                    // Submit the same form after the value is updated
                                    form.submit();
                                }
                            </script>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    
    <footer>
        <p>&copy; 2024 Twoja Aplikacja. Wszystkie prawa zastrzeżone.</p>
        <button onclick="location.href='@Url.Action("DyzurnyView", "Dyzurny")'">
            <img src="../back.png" alt="Powrót" />
        </button>
    </footer>
</body>

</html>
